var assert = require('assert');
var BeatBlocks = require('../src/beat-blocks');

describe("Basic Smoketests", function() {
  it("expects Beatblocks object to exist", function() {
    assert.ok(BeatBlocks);
  });
});
